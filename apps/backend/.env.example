# Comma-separated list. Use * to allow any origin (not recommended for production)
CORS_ORIGINS=http://localhost:4200
# Optional port override for running via __main__
PORT=8000
base_url=http://localhost:8000
